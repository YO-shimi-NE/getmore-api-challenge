# GetmoreApi

**TODO: Add description**
## Referencias
Programming Elixir ≥ 1.6
Functional |> Concurrent |> Pragmatic |> Fun

https://hexdocs.pm/elixir/1.12/File.html
https://hexdocs.pm/elixir/1.12/Enum.html#uniq/1

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `getmore_api` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:getmore_api, "~> 0.1.0"}
  ]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at [https://hexdocs.pm/getmore_api](https://hexdocs.pm/getmore_api).

